version: '3.8'

services:
  kasa:
    build: .
    ports:
      - "3131:3131"
    volumes:
      - ./data:/app/data
    environment:
      - DATABASE_URL=sqlite:///./data/kasa.db
    restart: unless-stopped
    networks:
      - kasa-network

networks:
  kasa-network:
    driver: bridge 